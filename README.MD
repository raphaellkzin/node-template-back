## Instalação dos pacotes
```pnpm i```


## Inicializa banco de dados
```docker-compose -f docker-compose.dev.yml up -d```
E preencha todo as variáveis de ambiente

## Inicializa banco de dados
```docker-compose -f docker-compose.dev.yml up -dado```
```pnpm prisma db push```
```pnpm db:seed```

## Inicializa APP
```pnpm dev```
